сайт книга (learn javascript) learn.javascript.ru
codebasiks.com
js может встраиваться в хтмл и выполн автомат при загрузке вэб странцы.
скрипты распространяются и выполняются как простой текст им не нужна спец подготовка
или компиляция для запуска.
сегодня js может выполняться не только в браузере, но и на сервере
или на другом любом устройстве.
ecmf-262 информация о js.
js не строго типизирован, поэтому на помощь приходит тайп скрипт.


alert("Привет, мир!") - в всплываюзем окне браузера
console.log("Привет, мир!") - появляется в консоли

// - комментарий
/* */ - комментирует то что внутри


1. ПЕРЕМЕННЫЕ - это именновоное хранилище для данных
в js есть 3 переменных
let - локальная переменная
var  - глобальная переменная
const - неизменяемая и непереопределяеммая переменная (констата)

let a=5
let b=3
c=a+b
console.log(c)

let var const - операторы объявления
по умолчанию пременные объявляются как глобальные (а = 5)


ТЕМА 2 "Типы данных"
пенрвый тип данных - числа (numbers)
любые целые и вещественные числа отн к числу

let a = 5;
let d = 3.14;
let c=a+b;
console.log(c)

второй тип данный - строковый (стринг)

let b = "3";

третий тип даных - булевые Boolean
let f = true;
let h = false;



четрвертый тип данных - массивы
let array = [3, 3.14, 'hello', false]


пятый тип данных - объект объект
let obj = {
    name: 'Dmitry',
    age: 27,
    email: 'grishin.d@mfua.ru'
    isActive: true
}

шестой тип данных - NULL
обозначение пустоты(явл ошибкой)

undefined - вводится когда есть неопределенная переменная или функция
переводится как - неопределена.


взаимодействие с пользователями, скрытые функции браузера.
alert() - всплывающее окно
prompt() - всплывающее окно для ввода данных
confirm() - всплывающее окно с подтверждением действия пользователя

промпт по умолчанию делает как строку

ПРЕОБРАЗОВАНИЕ ТИПОВ данных

let a = "123"
let num = Number(a)
console.log(typeof(num))

пример преобразования строковой в число

парс инт преобразует все в целое число
а парс флоат преобразует число в вещественное число

let a = "3.14"
let integer = parseInt(a)
let float = parseFloat(a)
let num = Number(a)
let bool = Boolean(a)

console.log(integer)
console.log(float)
console.log(num)
console.log(bool)


Математические операции
let a = 5
let b = 3

console.log(a + b)
console.log(a - b)
console.log(a * b)
console.log(a / b)
console.log( a ** b)
console.log(a % b)

програ запраш 2 числа с клавиатуры и потом она выводит результат умножения этих двух чисел в консоль
let a = +prompt("Введите первое число", "");
let b = prompt()
let c = a * b
console.log(c)


скрипт запрш у польз его возраст с клавиатуры а потом выводит в консоль результат с указанием сколько будет пользователю через 10 лет

let a = +prompt("Введите свой возраст", "");
let b = Number(10)
let c = a + b
console.log(c)


# Операторы сравнения # исп в случае если надо что то сравнить
> усл истинно если 1 оперант больше второго
<
<=
>=
== - условие истинно если оба оберанта равны без приведения к единому типу данных
=== (строгое равенство) - условие истинно если оба равны и имеют одинаковый тип данных
!= 

let a = 2
let b = 3
( a > b) = false (или true)

let a = '23'
let b = 23
console.log(a === b)
console.log(a == b)
console.log(a != b)
console.log(a !== b)


условные операторы. (if, else if, else)
if (условие) { блок кода 1}
else if (условие) {блок кода 2}
else {блок кода 3}
условные операторы позволяют выполнять различные действия в зависимости от выполнения или не выполнения определенных условий.

оператор if () исп для выполнения кода если указанные условия истинно.
оператор else if () используется для выполнения доп условий, если прошлые условия не были истинными.
оператор else нет условия, исп для выполнения кода, если ни одно из предыдущих усл не было выполнено.

{let x = +prompt()
let y = +prompt()
if (x > y) {console.log('x > y')}
else if (x < y) {console.log('x < y')}
else {console.log('x = y')}
}
задача на проверку двух чисел (+-=)

применение тернарных операторов
?
:
тернарные операторые упращ код но он позволяют реализовать только проверку двух условий
{let x = +prompt()
let y = +prompt()
let result=(x > y)? 'x > y' : 'x < или = y'
console.log(result)
}

задача программа запрашивает у пользователя какой сейчас год.
если пользователь вводит текущий код, то правильно
если не правильный, программа выдает что неправильно

let x = +prompt()
if (x > 2026) {console.log('неправильно')}
else if (x < 2026) {console.log('неправильно')}
else {console.log('правильно')}


тема логические операторы

1. && (и) (*) (умножение)
2. || (или) (+) (плюс)
3. ! (не) () (отрицание)

let a = true
let b = false
console.log((a && !b) || (!a || !b))

дз: напишите программу которая выводит в первом всплывающем окне введите логин
во втором спрашивает пароль
и если логин равен админ а пароль равен пассворд, то программа выведет успешный вход
ва остальный вариантах программа даст отказ

let login = prompt("Введите свой логин", "");
let pass = prompt("Введите свой пароль", "");
let admin = 'admin'
let password = 'password'
if (login === admin && pass === password) {alert('Успешный вход')}
else {alert('не правильно')}

прогр запраш у польз длины сторон треуг с клавиатуры и выводит сообщ о типе треугольника.

let a = +prompt("A - B", "");
let b = +prompt("B - C", "");
let c = +prompt("C - A", "");
if (a === 0 && b === 0 && c === 0) {console.log('нету')}
else if (a === b && b === c && c === a) {console.log('равносторонний')}
else if (a * a === b * b + c * c || b * b === a * a + c * c || c * c === b * b + a * a ) {console.log('прямоугольный')}
else if (a === 0 || b === 0 || c === 0) {console.log('нету')}
else if (a === b || b === c || a === c) {console.log('Равнобедренный')}
else if (a != b && b != c && a != c) {console.log('Разносторонний')}
else {console.log('прямоугольный')}

циклы while b for
при написании сриптов зачастую встает задача выполнить однотипное действие мнго раз
перебрать все числа от 1 до 10 и для всех выполнить одинаковый код (счетчик)
пример кода

let i = 0;
while (i<10) {
    i++
alert(i)
}

do while

let i = 0;
do {alert(i)
    i++
}
while(i < 0)

цикл фор состоит из 3 основных частей
1. инициализация
2. условие
3. действие



let x = +prompt("Введите x");
let y = +prompt("Введите y");
if (x > 0 && y > 0) {
    console.log("I четверть")
}
else if (x < 0 && y > 0) {
    console.log("II четверть")
}
else if (x < 0 && y < 0) {
    console.log("III четверть")
}
else if (x > 0 && y < 0) {
    console.log("IV четверть")
}
else { console.log("Начало координат")
}
 
 
Дописать условия на то, что точка лежит на оси Х или на оси У
 

прогр запраш число и выводит сумму всех чисел от 1 до этого числа с исп цикла фор

let x = +prompt("Введите x");
let y = +prompt("Введите y");

if (x === 0 && y === 0) {
    console.log("Начало координат");
} else if (x === 0) {
    console.log("Точка лежит на оси Y");
} else if (y === 0) {
    console.log("Точка лежит на оси X");
} else if (x > 0 && y > 0) {
    console.log("I четверть");
} else if (x < 0 && y > 0) {
    console.log("II четверть");
} else if (x < 0 && y < 0) {
    console.log("III четверть");
} else if (x > 0 && y < 0) {
    console.log("IV четверть");
}



let n = +prompt("Введите число:");
let sum = 0;

for (let i = 1; i <= n; i++) {
    sum = sum + i;
}

console.log("Сумма = " + sum);



прерывание цикла оператора break и continue
бреак - обычно цикл завершается при значении условия false но мы можем выйти из цикла условия с помощью break.

пример пименения оператора бреакпрограмма подсчитывает сумму подстч чисел, то тех пор пока пользоватеь их вводит

let sum = 0
while (true) {
    let value = +prompt()
    if (!value) break - прерывает
    sum = sum + value
}
alert(sum)


пример с континуе
создать цикл for чтобы вывести только не четные значения в диапозоне от 0 до 10

for (let i = 0; i <= 10; i++) {
    if (i % 2 == 0) continue
        alert(i);
    }



конструкция свич и кейс дефолт (switch case default)
явл аналогом цепочки иф элс

с клавиатуры ввод число, если равно 3 то пишет мало, если число равно 4 или 5, программа пишет в точку
если равно 6 то программа пишет много
если число любое другое программа пишет мимо

let a = +prompt()
    switch (a) {
        case 3: alert('мало')
        break
        case 4:
        case 5: alert('в точку')
        break
        case 6: alert('много')
        break
        default: alert('мимо')
    }



тема функций
задача:
с клавиатуры вводится 2 числа
если результат суммы этих 2 чисел равен 10, то програма выводит результат (10)
если результат суммы равен 15, то сумма равна 15
и если суммма равна 20 или 21, то она пишет результат равен 20 или 21
в остальных случаях (результат не интересует)

с помощью иф элс и с помощью свич кейс

let x = +prompt("Введите первое число");
let y = +prompt("Введите второе число");

if (x + y === 10) {
    alert('равно 10')
}

else if (x + y === 20 || x + y === 21) {
    alert("равно 20 или 21");
}
else if (x + y != 20 && x + y != 21 && x + y != 10 && x + y != 15) {
    alert("не интересует");
}

switch (x + y) {
    case 15: alert("равно 15");
    break
}



Тема функций
основные определения:
ЭТАПЫ
объявления функции
назначение параметров функции (не обязательно)
возврат значений (с помощью оператора return)
вызов функции в нужный момент

1 вариант написания функций
function myfunc(параметры - необязательно){блок кода функции}

function myfunc(){alert('hello, world')}
myfunc()

параметры функции это - это локальные переменные внутри функции
для дальнейшей работы с ними внутри себя (тела функции)
аргументы это некоторые переменные которые могут быть переданы в функцию извне

function myfunc(a, b){return a + b}
let result = myfunc(3,2)
console.log(result)


объекты массивы
массивы - сложная структура и отмечаются []
let array = [-3, 0, 3.14, 'string', true, '22'] //по индексу от 0 до 5, объектов 6
console.log(array[3]) //вывод элемента по индексу

метод пуш
array.push(10) - добавление элемента в конец массива
console.log(array.length) - показать кол-во объектов
array.shift - удаление первого элемента в начале
array.unshift('') - добавление элем в начало массива
array.pop() - удаляет последний элемент в массиве


//Массивы
let array = [-3, 0, 3.14, "string", true, "22", "string" ] //элементов 6. Индекс начинается с 0 (индексов 5)
//вывод значения элемента по индексу
// console.log(array.length) //вывод количества элементов в массиве
 
array.push("10") //добавление элемента в конец массива
console.log(array)
array.shift() //удаление первого элемента в массиве
console.log(array)
array.unshift("hello world") //добавляет элемент в начало массива
console.log(array)
array.pop() //удаляет последний элемент в массиве
console.log(array)
 
console.log(array.indexOf("string")) //возвращает индекс элемента со значением "string"


метод индексов
console.log(array.indexOf('string')) - возвращает индекс элемента со значением стринг


метод филтер - возвращает новый массивы содерж элементы которые выполняют условия (фильтрация)

const array = [-3, -1, 0, 3.14, 10, 20]
const newArray = array.filter(function(element) {
    if (element >= 0) {
        return true
    } 
    else {
        return false
    }
})

console.log(array)
console.log(newArray)

(6) [-3, -1, 0, 3.14, 10, 20]
(4) [0, 3.14, 10, 20]



const array = [-3, -1, 0, 3.14, 10, 20]
array.forEach(element => {
    element = element * 2
    console.log(element);
    })

console.log(array);

 -6
 -2
 0
 6.28
 20
 40
 (6) [-3, -1, 0, 3.14, 10, 20]



const array = [-3, -1, '0', 3.14, 10, 20]
const newArray = array.map(element => {
    return element * 2
    });

console.log(newArray)
console.log(newArray.includes(0))

(6) [-6, -2, 0, 6.28, 20, 40]
true


console.log(newArray.sort)


const array = [-3, -1, 0, 3.14, 10, 20]
const newArray = array.map(element => element * element)
console.log(newArray)
элементы массивы возводятся в квадрат

///создать массив который содержит все четные числа
///const newArray = array.filter(Element => Element % 2 === 0)
///console.log(newArray)


concat() - слияние массивов
const arrayFirst = [-3, -1, 0, 3.14, 10, 20]
const arraySecond = [30, 40, 50]
const arrayThird = [ -10, -20, -30]
const mixedArray = arrayFirst.concat(arraySecond).concat(arrayThird)
console.log(mixedArray)
[-3, -1, 0, 3.14, 10, 20, 30, 40, 50, -10, -20, -30]


slice() - срез массива
const sliceArray = arrayFirst.slice(3, 4) - с 3 индекса до 4
console.log(sliceArray)
[3.14]





МОДУЛЬ 3 РАБОТА С DOM
document.getElementById("first").style.background = "black"
document.getElementById("second").style.background = "orange"
document.getElementById("third").style.background = "white"
в скриптах

const first = document.getElementById("first")
first.style.background = "black"
first.style.color = "white"


querySelector - метод который вызывает через айди или класс

document.querySelector("[тег]") - возвращает первый элемент по тегу 
const first = document.querySelector("div")
first.style.background = "red"


///innerHTML = <h1>Заголовок<h2> - используется для получения HTML содержимого элемента
const first = document.querySelector("div")
first.innerHTML = "<h1>Hello</h1>"


AddEventListener('click", clicker) - метод, в котором написано событие и функция которая будет вызвана

const clicker=() => {
const h1 = document.getElementById("h1")
first.textContent = "Test"
first.style.color = "red"
first.style.fontsize = "100px"
}
const myBtn = document.querySelector('.myBtn')
myBtn.addEventListener('click', clicker)


setAttribute("", "") 1 - тип атрибума 2 - название атрибута


classList.toggle - добавляет класс если он отсутствует и наоборот
querySelectorAll - возвращает все элементы в виде массива

дз: Реализовать каждую задачу отдельной функцией в одном .js файле:

1) Изменение текста элемента при клике.
Создайте кнопку и элемент <p>. При клике на кнопку измените текст внутри <p> на фразу "Текст изменен" .
2) Переключение класса при наведении.
Создайте блок с текстом. При наведении на блок добавляйте ему класс, который меняет цвет фона. 
При уходе курсора удаляйте этот атрибут класс.
3) Переключение видимости элемента.
Создайте кнопку <button> и элемент <div>. По нажатию на кнопку скрывайте и показывайте элемент <div>
4) Изменение стилей элементов.
Создайте несколько блоков (div) и кнопок (button). Каждая из них должна изменять стиль (цвет фона, размер шрифта) 
какого-либо блока по нажатию на соответствующую кнопку.
5) Добавление и удаление элементов списка.
Создайте список <ul> с элементами списка (<li>). Добавьте текстовое поле и кнопку, 
при нажатии на которую добавляется новый элемент в список. Также рядом с каждым элементов создайте кнопку "Удалить", 
которая удаляет конкретный элемент из списка.
6) Изменение нескольких элементов на странице.
Сделайте таблицу с несколькими строками и столбцами. При нажатии на ячейку она должна менять свой цвет на зеленый. 
При повторном нажатии - возвращаться в исходное состояние.